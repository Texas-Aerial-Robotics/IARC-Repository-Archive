num=7;
mask=zeros(num,num);
shift=(num-1)/2;
for x=1:num
    
    for y=1:num
        
        mask(x,y)=(-1/(pi*sigma^4))*(1-((x^2+y^2)/(2*sigma^2)))...
            *exp(-(x^2+y^2)/(2*sigma^2));
        
    end
end
x=1:num;
y=1:num;
[X,Y]=meshgrid(x,y);
figure(1)
mesh(X,Y,mask)